apiVersion: v1
kind: Pod
metadata:
  name: deepdrone-train-1
spec:
  containers:
    - name: skipper-pod
      image: raminmh/deepdrone:latest
      command:
        - "/bin/bash"
        - "-c" # run with bash and pass in the args below
      args:
        - "python training.py \
        --model=lstm \
        --data_dir=/data/deepdrone/data \
        --save_dir=/data/deepdrone/logs \
        --history_dir=/data/deepdrone/history"
